{
  "scripts": {
    "format": "prettier --write './main/**/*.{ts,tsx,css,scss,json}' --ignore-path .prettierignore",
    "check-format": "prettier --check './main/**/*.{ts,tsx,css,scss,json}' --ignore-path .prettierignore"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
